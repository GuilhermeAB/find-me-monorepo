import { BaseEntityProps } from '../../base';
import { AlertImageProps } from './alert-image.entity';

export * from './alert-image.entity';

export interface AlertImageEntityType extends AlertImageProps, BaseEntityProps {}
